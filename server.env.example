# Copy this to server.env — NEVER commit server.env (it holds your secret API key)

GEMINI_API_KEY=your-google-gemini-api-key

# Allowed origin for CORS — set to your Vite dev URL in dev, Vercel URL in prod
ALLOWED_ORIGIN=http://localhost:5173

PORT=3001
